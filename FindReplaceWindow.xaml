<Window x:Class="UsersFile.FindReplaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Find & Replace" Height="200" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Find:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBox x:Name="FindTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" KeyDown="FindTextBox_KeyDown"/>
        
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Replace:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBox x:Name="ReplaceTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>
        
        <CheckBox x:Name="MatchCaseCheckbox" Grid.Row="2" Grid.Column="1" Content="Match Case" Margin="0,5"/>
        <CheckBox x:Name="MatchWholeWordCheckbox" Grid.Row="3" Grid.Column="1" Content="Match Whole Word" Margin="0,5"/>
        
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="FindNextButton" Content="Find Next" Click="FindNext_Click" Width="90" Margin="0,0,10,0"/>
            <Button x:Name="ReplaceButton" Content="Replace" Click="Replace_Click" Width="90" Margin="0,0,10,0"/>
            <Button x:Name="ReplaceAllButton" Content="Replace All" Click="ReplaceAll_Click" Width="90"/>
        </StackPanel>
    </Grid>
</Window>
